import { Command } from './index';
export declare class Program {
    env: string;
    constructor();
    run(message: any): void;
    print(this: Command & {
        reverse: boolean;
    }, first: string, last: string, credentials: string[]): void;
}
